// 1. Declaring a name and age
const name: string = "Petar Ivanov";
const age: number = 20;
console.log(`Hello! I am ${name}, ${age} years old!`);

// 2. Declaring two number values
const num1: number = 15;
const num2: number = 5;
const addition: number = num1 + num2;
const subtraction: number = num1 - num2;

console.log(`Addition: ${addition}`);
console.log(`Subtraction: ${subtraction}`);

// 3. Range check
const num: number = 25;
const isWithinRange: boolean = num >= 10 && num <= 50;

console.log(`Number ${num} is within range (10-50): ${isWithinRange}`);